<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FitCheck" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- Uncomment if needed
        -->
        <item name="colorPrimary">@color/navy</item>
        <item name="colorPrimaryVariant">@color/navy</item>
        <item name="android:statusBarColor">@color/navy</item>


    </style>

    <style name="Theme.FitCheck" parent="Base.Theme.FitCheck" />

    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/green</item>
        <item name="postSplashScreenTheme">@style/Theme.FitCheck</item>
    </style>
</resources>
